# Sessie <YYYY-MM-DD> — <korte titel>

> Kopieer naar `docs/session-<YYYY-MM-DD>-<slug>.md` en vul **tijdens** het
> werk in, niet achteraf. Doel: friction op de repo-docs vastleggen zodat
> een volgende (verse) sessie er niet opnieuw tegenaan loopt.

**Wat getest / gedaan:** <bv. clean bootstrap CP1 vanaf verse clone op nieuwe pc>
**Omgeving:** OS, `talosctl`-versie, `kubectl`-versie, verse clone ja/nee,
`~/.talos/config` leeg ja/nee.
**Startpunt in de docs:** `docs/from-scratch.md` §<n> / `talos/starstuff/README.md` §<n>

## Verloop

Per stap kort: wat stond in de doc → wat je deed → resultaat.

- §<n> … ✅ / ⚠️ / ❌ …

## Problemen

Per probleem: **symptoom** (exacte foutregel) → **oorzaak** → **fix** →
**doc-gap** (welke doc mist wat).

1. …

## Doc-gaps → issues

Klein lijstje met titel + 1 regel body. Aan het eind → `gh issue create`.

- [ ] <titel> — <wat er mist / moet veranderen>

## Lessen voor `AGENTS.md`

Gotchas die elke sessie moet weten (niet alleen deze repo-versie).

- …

## Overdracht

Wat is af, wat is de volgende concrete stap.
